{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "additionalProperties": false,
    "description": "A file of read sequences generated by a sequencing experiment.",
    "id": "https://schema.humancellatlas.org/type/file/5.1.0/sequence_file",
    "properties": {
        "describedBy": {
            "description": "The URL reference to the schema.",
            "pattern": "https://schema.humancellatlas.org/type/file/[0-9]{1,}.[0-9]{1,}.[0-9]{1,}/sequence_file",
            "type": "string"
        },
        "file_core": {
            "$ref": "https://schema.humancellatlas.org/core/file/5.1.0/file_core",
            "description": "Core file-level information.",
            "type": "object"
        },
        "insdc_run": {
            "description": "An INSDC (International Nucleotide Sequence Database Collaboration) run accession. Accession must start with DRR, ERR, or SRR.",
            "items": {
                "pattern": "^[D|E|S]RR[0-9]+$",
                "type": "string"
            },
            "type": "array",
            "user_friendly": "INSDC run"
        },
        "lane_index": {
            "description": "The index of the lane that this file was sequenced from.",
            "example": 1,
            "type": "integer",
            "user_friendly": "Lane index"
        },
        "read_index": {
            "description": "Whether the read file contains the read1, read2, index1, or index2 part of the sequencing read. If read file represents a single-end, non-indexed library, indicate that here.",
            "enum": [
                "read1",
                "read2",
                "index1",
                "index2",
                "single-end, non-indexed"
            ],
            "example": "read1",
            "type": "string",
            "user_friendly": "Read index"
        },
        "read_length": {
            "description": "The length of a sequenced read in this file, in nucleotides.",
            "example": 51,
            "type": "integer",
            "user_friendly": "Read length"
        },
        "schema_type": {
            "description": "The type of the metadata schema entity.",
            "enum": [
                "file"
            ],
            "type": "string"
        },
        "schema_version": {
            "description": "The version number of the schema in major.minor.patch format.",
            "example": "4.6.1",
            "pattern": "^[0-9]{1,}.[0-9]{1,}.[0-9]{1,}$",
            "type": "string"
        }
    },
    "required": [
        "describedBy",
        "schema_type",
        "file_core",
        "read_index"
    ],
    "title": "sequence_file",
    "type": "object"
}